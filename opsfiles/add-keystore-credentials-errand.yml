- type: replace
  path: /instance_groups/name=~opensearch.*/jobs/-
  value:
    name: add-keystore-credentials
    release: opensearch
    consumes:
      opensearch:
        from: opensearch_manager
        ip_addresses: true
    properties:
      opensearch.notifications.credential_map: ((opensearch-notifications-credential-map))

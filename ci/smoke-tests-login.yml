platform: linux

image_resource:
  type: docker-image
  source:
    repository: python
    tag: "3.6-alpine"

inputs:
- name: opensearch-config

run:
  path: sh
  args:
  - -exc
  - |
    pip install requests
    python opensearch-config/ci/smoke-tests-login.py

platform: linux

inputs:
- name: deploy-logs-opensearch-config

run:
  path: sh
  args:
  - -exc
  - |
    pip install deploy-logs-opensearch-config/ci/requirements.txt
    playwright install firefox
    python -m pytest deploy-logs-opensearch-config/ci --tracing retain-on-failure
    
platform: linux

inputs:
- name: deploy-logs-opensearch-config

run:
  path: sh
  args:
  - -exc
  - |
    python -m venv venv
    pip install -r deploy-logs-opensearch-config/ci/requirements.txt
    playwright install firefox
    ./deploy-logs-opensearch-config/scripts/e2e.sh
